package com.example.npone_llm.ui.theme

import androidx.compose.ui.graphics.Color

// === Palette principale (lumineuse et élégante) ===
val GoldYellow = Color(0xFFE8C602)      // Doré principal
val SoftBrown = Color(0xFF70704C)       // Gris olive profond (texte / contraste)
val DeepOlive = Color(0xFFC2B976)       // Brun doré clair (éléments secondaires)
val IvoryWhite = Color(0xFFFDFBF5)      // Fond général clair
val TextDark = Color(0xFF1E1C14)        // Texte foncé pour fond clair
val ButtonHover = Color(0xFFFFE55C)     // Doré clair pour hover
